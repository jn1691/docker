pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'Building docker image'
      }
    }
    stage('Test') {
      steps {
        echo 'testing docker image'
      }
    }
    stage('Publish') {
      steps {
        echo 'Ready to publish'
      }
    }
  }
}